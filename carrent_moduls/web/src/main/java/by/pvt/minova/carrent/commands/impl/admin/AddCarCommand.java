package by.pvt.minova.carrent.commands.impl.admin;

import by.pvt.minova.carrent.commands.AbstractCommand;

import by.pvt.minova.carrent.entities.Car;
import javax.servlet.http.HttpServletRequest;

public class AddCarCommand extends AbstractCommand {
    private Car car;

    @Override
    public String execute(HttpServletRequest request) {
        // команда НЕ реализована

        String page = null;

        return page;
    }
}
